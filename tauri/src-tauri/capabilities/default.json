{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability granted to the main application window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "log:default",
    "updater:default"
  ]
}
